name: "release-tauri-app"
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-rc.*"

jobs:
  publish-happ:
    name: Stage 1 - publish happ
    permissions:
      contents: write
    runs-on: ubuntu-22.04
    outputs:
      releaseId: ${{ steps.create-release.outputs.id }}
      appVersion: ${{ steps.version.outputs.APP_VERSION }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_PAT }}
          submodules: recursive
          # Full history: a UI release diffs the parent tag's submodule pointer.
          fetch-depth: 0

      - name: Install nix
        uses: cachix/install-nix-action@v27
        with:
          github_access_token: ${{ secrets.GIT_PAT }}
          nix_path: nixpkgs=channel:nixos-24.05

      - uses: cachix/cachix-action@v15
        with:
          name: holochain-ci

      - uses: cachix/cachix-action@v15
        with:
          name: darksoil-studio

      - name: Assert version contract (tag == src-tauri Cargo.toml == tauri.conf.json)
        id: version
        env:
          TAG: ${{ github.ref_name }}
        run: |
          APP_VERSION="$(bash scripts/check-version-contract.sh "$TAG")"
          echo "APP_VERSION=$APP_VERSION" >> "$GITHUB_OUTPUT"

      - name: Resolve release kind (tag patch → migration | UI)
        id: kind
        env:
          TAG: ${{ github.ref_name }}
        run: bash scripts/release-kind.sh "$TAG" >> "$GITHUB_OUTPUT"

      - name: Build DNA + happ (migration release, vM.m.0)
        if: steps.kind.outputs.kind == 'migration'
        run: |
          cd unyt && nix develop --no-update-lock-file --accept-flake-config --command bash -c "make package"

      - name: Inherit lineage happ + repack UI (UI release, vM.m.p)
        if: steps.kind.outputs.kind == 'ui'
        env:
          GH_TOKEN: ${{ secrets.GIT_PAT }}
          TAG: ${{ github.ref_name }}
          PARENT_TAG: ${{ steps.kind.outputs.parent_tag }}
        run: bash scripts/inherit-ui-release.sh "$TAG" "$PARENT_TAG"

      - name: Get Changelog Content
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.APP_VERSION }}"
          awk -v ver="$VERSION" '
            $0 ~ "^## \\[" ver "\\]" {p=1; next}
            p && /^## \[/ {exit}
            p {print}
          ' unyt/CHANGELOG.md > release_notes.txt
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          cat release_notes.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - id: create-release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          # Declared-but-unbuilt artifacts must fail, not slip through.
          artifactErrorsFailBuild: true
          artifacts: |
            unyt/workdir/unyt.happ
            unyt/workdir/unyt.webhapp
            unyt/dnas/alliance/workdir/alliance.dna
          body: ${{ steps.changelog.outputs.notes }}
          # The EXACT pushed tag, so an -rc.* push cannot become a stable vM.m.p
          # release the router would accept. APP_VERSION still names the assets.
          name: Unyt Sandbox ${{ github.ref_name }}
          tag: ${{ github.ref_name }}
          prerelease: true
          draft: true

  release-tauri-app:
    name: Stage 2 - release tauri apps (${{ matrix.config.label }})
    needs: publish-happ
    strategy:
      fail-fast: false
      matrix:
        config:
          - platform: macos-latest
            args: "--target aarch64-apple-darwin"
            arc_factor: default
            label: macos-arm64
          - platform: macos-latest
            args: "--target x86_64-apple-darwin"
            arc_factor: default
            label: macos-x64
          - platform: ubuntu-22.04
            args: "--bundles deb,appimage"
            arc_factor: default
            label: linux
          - platform: windows-latest
            args: ""
            arc_factor: default
            label: windows
          # - platform: macos-latest
          #   args: "--target aarch64-apple-darwin"
          #   arc_factor: "zero"
          # - platform: macos-latest
          #   args: "--target x86_64-apple-darwin"
          #   arc_factor: "zero"
          # - platform: ubuntu-22.04
          #   args: "--bundles deb,appimage"
          #   arc_factor: "zero"
          # - platform: windows-latest
          #   args: ""
          #   arc_factor: "zero"
    env:
      MACOSX_DEPLOYMENT_TARGET: 10.13
      HOLOCHAIN_ARC_FACTOR: ${{ matrix.config.arc_factor != 'default' &&
        matrix.config.arc_factor || '' }}
      CARGO_PROFILE_RELEASE_CODEGEN_UNITS: "16"
      CARGO_PROFILE_RELEASE_LTO: "false"
      # Vite embeds this at build time; empty means the shipped app can never
      # check for updates on either channel. Public endpoint, not a credential.
      VITE_MIGRATION_SERVICE_URL: https://migration.unyt.dev
    permissions: write-all

    runs-on: ${{ matrix.config.platform }}
    steps:
      - name: Support longpaths
        if: matrix.config.platform != 'ubuntu-22.04'
        run: git config --system core.longpaths true

      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_PAT }}
          submodules: recursive
          fetch-depth: 1

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: install Rust 1.95.0
        # A floor, not a preference: holochain 0.7.0 needs `cfg_select!` (1.95).
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.95.0

      - name: install x86_64 target
        if: matrix.config.args == '--target x86_64-apple-darwin'
        run: rustup target add x86_64-apple-darwin

      - name: install dependencies (ubuntu only)
        if: matrix.config.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev \
            build-essential \
            curl \
            wget \
            file \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            javascriptcoregtk-4.1 \
            libsoup-3.0 \
            webkit2gtk-4.1

      - name: Install jq on Windows
        if: runner.os == 'Windows'
        run: choco install -y jq

      - name: Install and prepare
        working-directory: unyt
        # `--ignore-engines` as in the White-label UI workflow: hc-spin's native
        # helper wants node >= 24, node here is 22, and yarn aborts the WHOLE
        # install — which is how v0.101.0-rc.0 produced no installers at all.
        # Node 22 is deliberate: the app's own floor and what the nix shell ships.
        # `--frozen-lockfile` pins this install only — `make ci-build-ui` re-runs
        # yarn unpinned inside the build.
        run: |
          yarn install --frozen-lockfile --ignore-engines

      - name: Download release of the .happ
        uses: robinraju/release-downloader@v1.10
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_PAT }}
        with:
          token: ${{ secrets.GIT_PAT }}
          releaseId: ${{ needs.publish-happ.outputs.releaseId }}
          fileName: "unyt.happ"
          out-file-path: "unyt/workdir/"

      - name: Assert migration-service URL is set (no silently un-updatable build)
        shell: bash
        run: |
          [ -n "$VITE_MIGRATION_SERVICE_URL" ] || {
            echo "::error::VITE_MIGRATION_SERVICE_URL is empty — a shipped build without it can never check for updates (both channels short-circuit to 'unreachable')."
            exit 1
          }

      - name: build (${{ matrix.config.arc_factor }} arc)
        uses: tauri-apps/tauri-action@v0
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_DEV_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID_EMAIL }}
          APPLE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GIT_PAT }}
          VITE_COMMIT_SHA: Unyt-Sandbox-${{ needs.publish-happ.outputs.appVersion }}-${{
            matrix.config.arc_factor }}-arc
          APP_VERSION: ${{ needs.publish-happ.outputs.appVersion }}
          HOLOCHAIN_ARC_FACTOR: ${{ matrix.config.arc_factor != 'default' &&
            matrix.config.arc_factor || '' }}
          UNYT_JOINING_SERVICE_URL: https://joining.unyt.dev
          VITE_CENTRALIZED_CONFIG_URL: https://config.unyt.dev
          VITE_MIGRATION_SERVICE_URL: ${{ env.VITE_MIGRATION_SERVICE_URL }}
        with:
          projectPath: unyt
          releaseId: ${{ needs.publish-happ.outputs.releaseId }}
          releaseDraft: true
          prerelease: true
          args: ${{ matrix.config.args }}
          assetNamePattern: ${{
            format('unyt_[version]_[name]_{0}-arc_[arch]_[platform][ext]',
            matrix.config.arc_factor) }}

  # Called directly, not via `workflow_run`, which only fires for workflow files
  # on the DEFAULT branch and so would never run on a release branch.
  #
  # Nothing `needs` this job — do not add it to another job's `needs`. That keeps
  # it from blocking a release; `non-blocking` below is what keeps a red check
  # out of the run's CONCLUSION. Neither extends to the build jobs: a build that
  # fails must still fail the run.
  #
  # Each lane skips when its own artifact is absent, so a failed Windows build
  # costs the Windows lane and nothing else.
  smoke-test:
    name: Stage 3 - smoke the artifacts
    needs: [publish-happ, release-tauri-app]
    # NOT gated on the build job: `needs` alone skips this whenever ANY matrix
    # row failed, which would suppress a good Linux check for a Windows failure.
    # `!cancelled()` rather than `always()`: a cancelled run should take the
    # smoke with it, not start a 30-minute container sweep nobody is waiting for.
    if: ${{ !cancelled() && needs.publish-happ.outputs.releaseId != '' }}
    uses: ./.github/workflows/release-smoke.yaml
    with:
      release: ${{ needs.publish-happ.outputs.releaseId }}
      # Set ONLY here: a hand-dispatched release-smoke must answer red.
      non-blocking: true
    # Explicit, not `inherit`: the build above holds the Apple signing
    # credentials and the Tauri signing key, which this job must not see.
    secrets:
      GIT_PAT: ${{ secrets.GIT_PAT }}

  # release-tauri-android-app:
  #   needs: publish-happ
  #   permissions: write-all
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       app_variant: ${{ fromJson(needs.publish-happ.outputs.app_variants) }}
  #   env:
  #     PRODUCT_NAME: ${{ matrix.app_variant.product_name }}
  #     TAURI_PRODUCT_NAME: ${{ matrix.app_variant.product_name }}
  #     TAURI_APP_IDENTIFIER: ${{ matrix.app_variant.identifier }}
  #     TAURI_APP_ID_PREFIX: ${{ matrix.app_variant.id }}
  #     TAURI_DEEP_LINK_SCHEME: ${{ matrix.app_variant.id }}
  #     TAURI_SPLASHSCREEN_TITLE: ${{ matrix.app_variant.product_name }} Loading
  #     TAURI_APP_VARIANT: ${{ matrix.app_variant.id }}
  #   runs-on: ubuntu-22.04
  #   steps:
  #     - name: Maximize build space
  #       uses: AdityaGarg8/remove-unwanted-software@v5
  #       with:
  #         remove-android: "true"
  #         remove-dotnet: "true"
  #         remove-haskell: "true"
  #         remove-codeql: "true"
  #         remove-docker-images: "true"
  #         remove-large-packages: "true"
  #         remove-cached-tools: "true"

  #     - uses: actions/checkout@v4
  #       with:
  #         token: ${{ secrets.GIT_PAT }}
  #         submodules: recursive
  #         fetch-depth: 0

  #     - name: setup node
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: 20

  #     - name: Install and prepare
  #       run: yarn install

  #     - name: Prepare app variant (config + icons)
  #       run: |
  #         chmod +x scripts/generate-tauri-config.sh scripts/copy-app-icons.sh
  #         bash scripts/generate-tauri-config.sh
  #         bash scripts/copy-app-icons.sh

  #     - name: Retrieve version
  #       run: |
  #         echo "APP_VERSION=$(jq -r '.version' src-tauri/tauri.conf.json)" >> $GITHUB_OUTPUT
  #       id: version

  #     - name: Download release of the .happ
  #       uses: robinraju/release-downloader@v1.10
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GIT_PAT }}
  #       with:
  #         token: ${{ secrets.GIT_PAT }}
  #         releaseId: ${{ needs.publish-happ.outputs.releaseId }}
  #         fileName: "unyt.happ"
  #         out-file-path: "workdir/"

  #     - name: install Rust stable
  #       uses: actions-rs/toolchain@v1
  #       with:
  #         override: true
  #         toolchain: 1.95.0

  #     - name: install Go stable
  #       uses: actions/setup-go@v4
  #       with:
  #         go-version: "stable"

  #     - name: Install nix
  #       uses: cachix/install-nix-action@v27
  #       with:
  #         github_access_token: ${{ secrets.GIT_PAT }}
  #         nix_path: nixpkgs=channel:nixos-24.05

  #     - uses: cachix/cachix-action@v15
  #       with:
  #         name: holochain-ci

  #     - uses: cachix/cachix-action@v15
  #       with:
  #         name: darksoil-studio

  #     - name: Regenerate Android project for variant
  #       run: |
  #         rm -rf src-tauri/gen/android
  #         nix develop --accept-flake-config .#androidDev --command bash -c "yarn tauri android init"

  #     - name: Set usesCleartextTraffic to true
  #       run: |
  #         chmod +x scripts/android-uses-cleartext-traffic.sh
  #         bash scripts/android-uses-cleartext-traffic.sh

  #     - name: setup Android signing
  #       run: |
  #         cd src-tauri/gen/android
  #         echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}" > key.properties
  #         echo "password=${{ secrets.ANDROID_KEY_PASSWORD }}" >> key.properties
  #         base64 -d <<< "${{ secrets.ANDROID_KEY_BASE64 }}" > $RUNNER_TEMP/keystore.jks
  #         echo "storeFile=$RUNNER_TEMP/keystore.jks" >> key.properties

  #     - name: Install and Build (zero arc)
  #       env:
  #         VITE_COMMIT_SHA: ${{ env.PRODUCT_NAME }}-${{ steps.version.outputs.APP_VERSION }}-android
  #         APP_VERSION: ${{ steps.version.outputs.APP_VERSION }}
  #         HOLOCHAIN_ARC_FACTOR: "0"
  #         TAURI_APP_IDENTIFIER: ${{ matrix.app_variant.identifier }}
  #         TAURI_APP_ID_PREFIX: ${{ matrix.app_variant.id }}
  #       run: |
  #         nix develop --accept-flake-config .#androidDev --command bash -c "make build-android-release"

  #     - name: Copy AAB for upload (variant-specific name)
  #       run: |
  #         mkdir -p upload
  #         AAB="$(find src-tauri/gen/android/app/build/outputs/bundle -name '*.aab' 2>/dev/null | head -1)"
  #         [ -n "$AAB" ] && cp "$AAB" "upload/${{ matrix.app_variant.id }}-${{ steps.version.outputs.APP_VERSION }}.aab"

  #     - name: Upload release aab
  #       uses: AButler/upload-release-assets@v3.0
  #       with:
  #         files: upload/*.aab
  #         repo-token: ${{ secrets.GIT_PAT }}
  #         release-id: ${{ needs.publish-happ.outputs.releaseId }}

  #     - name: Copy APK for upload (variant-specific names)
  #       run: |
  #         mkdir -p upload-apk
  #         i=0
  #         while IFS= read -r f; do
  #           [ -f "$f" ] && cp "$f" "upload-apk/${{ matrix.app_variant.id }}-${{ steps.version.outputs.APP_VERSION }}-$i.apk" && i=$((i+1))
  #         done < <(find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" 2>/dev/null)

  #     - name: Upload release apk
  #       uses: AButler/upload-release-assets@v3.0
  #       with:
  #         files: upload-apk/*.apk
  #         repo-token: ${{ secrets.GIT_PAT }}
  #         release-id: ${{ needs.publish-happ.outputs.releaseId }}
