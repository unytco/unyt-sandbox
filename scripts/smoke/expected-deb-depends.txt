# The shared-library dependencies `dpkg-shlibdeps` computes from the shipped
# binary — the packages the app ACTUALLY needs, one per line, sorted.
#
# This file is the review gate. check-deb-depends.sh recomputes the set on every
# run and fails on any difference, so a change in what the binary links against
# has to be acknowledged by a human editing this file rather than shipping
# silently. Regenerate with:
#
#   scripts/smoke/run-smoke.sh --print-computed-depends
#
# Captured from unyt_0.100.0_Unyt.Sandbox_default-arc_amd64_linux.deb on
# ubuntu:22.04. Note `libc6 (>= 2.34)`: that is the constraint whose absence from
# the package's declared Depends lets the app install on a too-old glibc and then
# fail at exec — see check-deb-depends.sh for the whole story.
libc6 (>= 2.34)
libcairo-gobject2 (>= 1.10.0)
libcairo2 (>= 1.10.0)
libgcc-s1 (>= 4.2)
libgdk-pixbuf-2.0-0 (>= 2.36.9)
libglib2.0-0 (>= 2.65.1)
libgtk-3-0 (>= 3.21.5)
libjavascriptcoregtk-4.1-0
libpango-1.0-0 (>= 1.14.0)
libsoup-3.0-0 (>= 3.0.3)
libwebkit2gtk-4.1-0 (>= 2.41.90)
